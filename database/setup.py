from setuptools import setup, find_packages  
setup(name = 'essential_function', packages = find_packages())
setup(name = 'browser_control', packages = find_packages())
setup(name = 'features_function', packages = find_packages())
setup(name = 'main_function', packages = find_packages())